

class gapAlgorithm:
    def __init__(self, distances, vestInfo):
        #things we need are some constants
        #size of distance array? ->-> it should get this from the dd map
        #maximum distance?

        #TODO:
        #this should take in the distances from depth
        #this should take in some similar array with vestVotes (1-hot)
        #need to come up with some way to normalize this information
        self.distances = distances;
        self.MAX_DIST = 3;

    def preprocess_dists():
        return;

    def find_gaps():
        
        return;

    def find_best_point():
        return;



        